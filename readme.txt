1. create new virtual environment "py -m venv env"
2. activate the environment "env/scripts/activate"
3. install all needed requirements "py -m pip install -r requirements.txt"
4. run the script "py -m main"
